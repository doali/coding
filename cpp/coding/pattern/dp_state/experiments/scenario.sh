#!/usr/bin/bash

readonly BUILD=build
readonly SRC=src
readonly EXE=sm

# ------------------------------------------------------------------------------
[ -d ${BUILD} ] && rm -rf ${BUILD}
mkdir ${BUILD}

# ------------------------------------------------------------------------------
cd ${BUILD}

cmake ../${SRC}
make

# ------------------------------------------------------------------------------
./${EXE}
